import CustomCakes from "@/components/CustomCakes";

export const metadata = {
  title: "Custom Cakes | Kake N Kream",
  description: "Request a custom cake in St. Charles, MO for birthdays, baby showers, and special celebrations. Handcrafted with fresh ingredients and elegant designs.",
};

export default function CustomCakesPage() {
  return <CustomCakes />;
}
